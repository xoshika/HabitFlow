services:
  - type: web
    name: habitfloww-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --chdir backend app:app
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: habitfloww-db
          property: connectionString
      - key: PYTHON_VERSION
        value: 3.10.0

databases:
  - name: habitfloww-db
    plan: free
